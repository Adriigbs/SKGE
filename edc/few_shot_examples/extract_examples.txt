EXAMPLE 1:

Text:
I am playing with a set of blocks where I need to arrange the blocks into 
stacks. Here are the actions I can do

Pick up a block
Unstack a block from on top of another block
Put down a block
Stack a block on top of another block

I have the following restrictions on my actions:
I can only pick up or unstack one block at a time.
I can only pick up or unstack a block if my hand is empty.
I can only pick up a block if the block is on the table and the block is 
clear.
I can only unstack a block from on top of another block if the block I am 
unstacking was really on top of the other block.
I can only unstack a block from on top of another block if the block I am 
unstacking is clear.
Once I pick up or unstack a block, I am holding the block.
I can only put down a block that I am holding.
I can only stack a block on top of another block if I am holding the block 
being stacked.
I can only stack a block on top of another block if the block onto which I 
am stacking the block is clear.
Once I put down or stack a block, my hand becomes empty.
Once you stack a block on top of a second block, the second block is no 
longer clear.

As initial conditions I have that the red block is clear, the blue block is 
clear, the hand is empty, the red block is on top of the orange block, the 
blue block is on the table and the orange block is on the table.
My goal is to have that the red block is on top of the blue block.

CONSTRAINT TRIPLES:
[pickup(X), requires, on(X, table)]
[pickup(X), requires, clear(X)]
[pickup(X), requires, not(holds(hand, X))]
[pickup(X), adds, holds(hand, X)]
[pickup(X), removes, on(X, table)]
[pickup(X), removes, clear(X)]
[unstack(X,Y), requires, on(X,Y)]
[unstack(X,Y), requires, clear(X)]
[unstack(X,Y), requires, not(holds(hand, X))]
[unstack(X,Y), adds, holds(hand, X)]
[unstack(X,Y), adds, clear(Y)]
[unstack(X,Y), removes, on(X,Y)]
[unstack(X,Y), removes, clear(X)]
[putdown(X), requires, holds(hand, X)]
[putdown(X), adds, on(X, table)]
[putdown(X), adds, clear(X)]
[putdown(X), removes, holds(hand, X)]
[stack(X,Y), requires, holds(hand, X)]
[stack(X,Y), requires, clear(Y)]
[stack(X,Y), adds, on(X,Y)]
[stack(X,Y), removes, holds(hand, X)]
[stack(X,Y), removes, clear(Y)]
[clear(X), requires, not(on(Y,X))]
[clear(X), requires, not(holds(hand,X))]

STATE TRIPLES:
[red_block, on, orange_block]
[blue_block, on, table]
[orange_block, on, table]
[red_block, is, clear]
[blue_block, is, clear]

GOAL TRIPLES:
[red_block, on, blue_block]

EXAMPLE 2:

Text:
I am playing with a set of blocks where I need to arrange the blocks into 
stacks. Here are the actions I can do

Pick up a block
Unstack a block from on top of another block
Put down a block
Stack a block on top of another block

I have the following restrictions on my actions:
I can only pick up or unstack one block at a time.
I can only pick up or unstack a block if my hand is empty.
I can only pick up a block if the block is on the table and the block is 
clear.
I can only unstack a block from on top of another block if the block I am 
unstacking was really on top of the other block.
I can only unstack a block from on top of another block if the block I am 
unstacking is clear.
Once I pick up or unstack a block, I am holding the block.
I can only put down a block that I am holding.
I can only stack a block on top of another block if I am holding the block 
being stacked.
I can only stack a block on top of another block if the block onto which I 
am stacking the block is clear.
Once I put down or stack a block, my hand becomes empty.
Once you stack a block on top of a second block, the second block is no 
longer clear.

As initial conditions I have that the yellow block is clear, the hand is 
empty, the yellow block is on top of the green block, the green block is on 
top of the blue block and the blue block is on the table.
My goal is to have that the blue block is on top of the yellow block and 
the green block is on top of the blue block.

CONSTRAINT TRIPLES:
[pickup(X), requires, on(X, table)]
[pickup(X), requires, clear(X)]
[pickup(X), requires, not(holds(hand, X))]
[pickup(X), adds, holds(hand, X)]
[pickup(X), removes, on(X, table)]
[pickup(X), removes, clear(X)]
[unstack(X,Y), requires, on(X,Y)]
[unstack(X,Y), requires, clear(X)]
[unstack(X,Y), requires, not(holds(hand, X))]
[unstack(X,Y), adds, holds(hand, X)]
[unstack(X,Y), adds, clear(Y)]
[unstack(X,Y), removes, on(X,Y)]
[unstack(X,Y), removes, clear(X)]
[putdown(X), requires, holds(hand, X)]
[putdown(X), adds, on(X, table)]
[putdown(X), adds, clear(X)]
[putdown(X), removes, holds(hand, X)]
[stack(X,Y), requires, holds(hand, X)]
[stack(X,Y), requires, clear(Y)]
[stack(X,Y), adds, on(X,Y)]
[stack(X,Y), removes, holds(hand, X)]
[stack(X,Y), removes, clear(Y)]
[clear(X), requires, not(on(Y,X))]
[clear(X), requires, not(holds(hand,X))]

STATE TRIPLES:
[yellow_block, on, green_block]
[green_block, on, blue_block]
[blue_block, on, table]
[yellow_block, is, clear]

GOAL TRIPLES:
[blue_block, on, yellow_block]
[green_block, on, blue_block]